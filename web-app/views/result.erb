<h1>Palindrome Result</h1>

<% if @phrase.palindrome? %>
  <div class="result result-success">
    <p>"<%= @phrase %>" is a palindrome!</p>
  </div>
<% else %>
  <div class="result result-fail">
    <p>"<%= @phrase %>" isn't a palindrome!</p>
  </div>
<% end %>

<h2> Try  anotther one </h2>

<%= erb :palindrome_form %>
